datasource:
  default: ${APP_ENV}
  development:
    username: ""
    password: ""
    url: "jdbc:h2:./database"
  production:
    username: ${PGUSER}
    password: ${PGPASSWORD}
    url: "jdbc:postgresql://containers-us-west-101.railway.app:7153/postgres"

ebean:
  migration:
    run: true
production:
  migration:
    run: true